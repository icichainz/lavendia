<?xml version="1.0" encoding="utf-8"?>
<!--
    Default (release/profile) network policy.

    Cleartext HTTP is denied everywhere except the two non-routable loopback
    hosts used to reach a developer machine's Django server:
      - 10.0.2.2  : the Android emulator's alias for the host machine
      - localhost : reverse-tethered devices (adb reverse tcp:8000 tcp:8000)

    Neither address can reach the public internet, so allowing them does not
    weaken production traffic. Any real API host must be https://.

    Debug builds use a more permissive override in
    android/app/src/debug/res/xml/network_security_config.xml so that a
    physical device can hit a LAN IP during development.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
